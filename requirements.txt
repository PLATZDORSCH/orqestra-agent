# Install: pip install -r requirements.txt
# Runtime dependencies are defined in pyproject.toml; this file pins the editable install.
# For local dev with tests: pip install -e ".[analysis,browser,test]"
-e ".[analysis,browser]"
