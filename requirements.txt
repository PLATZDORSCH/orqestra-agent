openai>=1.0
python-frontmatter>=1.0
trafilatura>=1.0
httpx>=0.25
pyyaml>=6.0

# Optional: pip install -e ".[browser]" && playwright install chromium — DOM SEO (analyze_page_seo)
#
# Optional: extended data analysis, charts, and reports
# pip install -e ".[analysis]" for all of these:
# pandas>=2.0
# matplotlib>=3.7
# openpyxl>=3.1
# numpy>=1.24
